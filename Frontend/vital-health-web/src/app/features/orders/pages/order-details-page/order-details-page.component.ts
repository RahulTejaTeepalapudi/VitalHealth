import { CurrencyPipe } from '@angular/common';import { Component,inject } from '@angular/core';import { ActivatedRoute,RouterLink } from '@angular/router';import { OrdersService } from '../../services/orders.service';
@Component({selector:'app-order-details-page',imports:[CurrencyPipe,RouterLink],templateUrl:'./order-details-page.component.html',styleUrl:'./order-details-page.component.scss'})export class OrderDetailsPageComponent{readonly service=inject(OrdersService);private readonly route=inject(ActivatedRoute);readonly order=this.service.getOrder(this.route.snapshot.paramMap.get('id')??'')??this.service.getOrders()[0];readonly total=this.service.getTotal(this.order);}
