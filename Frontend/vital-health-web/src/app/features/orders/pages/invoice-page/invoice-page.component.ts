import { CurrencyPipe } from '@angular/common';import { Component,inject } from '@angular/core';import { ActivatedRoute,RouterLink } from '@angular/router';import { OrdersService } from '../../services/orders.service';
@Component({selector:'app-invoice-page',imports:[CurrencyPipe,RouterLink],templateUrl:'./invoice-page.component.html',styleUrl:'./invoice-page.component.scss'})export class InvoicePageComponent{readonly service=inject(OrdersService);private readonly route=inject(ActivatedRoute);readonly order=this.service.getOrder(this.route.snapshot.paramMap.get('id')??'')??this.service.getOrders()[0];readonly total=this.service.getTotal(this.order);print(){window.print()}download(){const content=`VitalHealth Invoice ${this.order.invoiceNumber}\nOrder ${this.order.orderNumber}\nTotal $${this.total.toFixed(2)}`;const url=URL.createObjectURL(new Blob([content],{type:'text/plain'}));const anchor=document.createElement('a');anchor.href=url;anchor.download=`${this.order.invoiceNumber}.txt`;anchor.click();URL.revokeObjectURL(url)}}
