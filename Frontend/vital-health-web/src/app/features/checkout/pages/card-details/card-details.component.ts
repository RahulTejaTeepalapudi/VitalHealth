import { Component, inject } from '@angular/core';import { FormControl,FormGroup,ReactiveFormsModule,Validators } from '@angular/forms';import { Router,RouterLink } from '@angular/router';import { CheckoutStepperComponent } from '../../components/checkout-stepper/checkout-stepper.component';import { CheckoutService } from '../../services/checkout.service';
@Component({selector:'app-card-details',imports:[ReactiveFormsModule,RouterLink,CheckoutStepperComponent],templateUrl:'./card-details.component.html',styleUrl:'./card-details.component.scss'}) export class CardDetailsComponent{readonly checkout=inject(CheckoutService);readonly form=new FormGroup({cardholderName:new FormControl(this.checkout.card().cardholderName,{nonNullable:true,validators:Validators.required}),cardNumber:new FormControl(this.checkout.card().cardNumber,{nonNullable:true,validators:Validators.required}),expiry:new FormControl(this.checkout.card().expiry,{nonNullable:true,validators:Validators.required}),securityCode:new FormControl(this.checkout.card().securityCode,{nonNullable:true,validators:Validators.required}),saveCard:new FormControl(this.checkout.card().saveCard,{nonNullable:true})});constructor(private readonly router:Router){}submit(){if(this.form.invalid){this.form.markAllAsTouched();return}this.checkout.card.set(this.form.getRawValue());void this.router.navigateByUrl('/checkout/review')}}
